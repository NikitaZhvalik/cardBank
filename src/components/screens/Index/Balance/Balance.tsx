import { FC } from "react";
import styles from "./styles.module.scss"

const Balance: FC = () => {
    return (
        <div className={styles.balance}>Balance lore</div>
    );
}
 
export default Balance;